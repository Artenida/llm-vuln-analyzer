llm:
  provider: openai
  model: gpt-4o-mini
  max_tokens: 2000
  temperature: 0          # deterministic output

ingestion:
  max_function_lines: 200
  skip_dirs:
    - node_modules
    - .git
    - __pycache__
    - vendor
    - dist
    - build
    - .next
    - coverage

output:
  results_folder: experiments/results

  extraction_folder: experiments/results/extraction

  context_folder: experiments/results/context

  analysis_folder: experiments/results/analysis

  evaluation_folder: experiments/results/evaluation

  save_per_run: true